import * as React from 'react';

import PageNotFound from './views/components/PageNotFound';

import './styles/main.scss';

const Error: React.FC = (): React.ReactElement => <PageNotFound />;

export default Error;
